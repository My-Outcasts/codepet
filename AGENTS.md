# Codepet — Project Instructions

See **[CLAUDE.md](CLAUDE.md)**. It is the single set of project instructions for this repo and applies to every agent, whatever the filename its harness looks for.

This file is deliberately a pointer, not a copy. It used to be a byte-identical duplicate of CLAUDE.md, and the duplicate went stale: it still described Codepet as a learning game, named a second checkout as the source of truth for Cloud Functions, and claimed a macOS 13 deployment target. Two files describing one project drift, and the stale one gets believed.
